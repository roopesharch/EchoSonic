syntax = "proto3";

package voice;

// Ensure Go can find the package
option go_package = "./proto";

service VoiceService {
  rpc Speak (SpeakRequest) returns (SpeakResponse);
}

message SpeakRequest {
  string text = 1;
}

message SpeakResponse {
  bool success = 1;
  string message = 2;
}